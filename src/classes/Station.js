export default class Station {
  constructor(
    stopId,
    latitude,
    longitude,
    name
  ) {
    this.stopId = stopId;
    this.latitude = latitude;
    this.longitude = longitude;
    this.name = name
  }

}